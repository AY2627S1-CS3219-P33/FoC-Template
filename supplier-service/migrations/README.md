# Database migrations

Add ordered Goose SQL migrations here when the supplier persistence model is
implemented. Migration files should use names such as
`00001_create_suppliers.sql` and include both `-- +goose Up` and
`-- +goose Down` sections.
