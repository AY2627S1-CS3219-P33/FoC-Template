operations:
  readiness:
    operationId: getReadiness
    tags: [Operations]
    summary: Check whether required supplier operations can run
    security: []
    responses:
      "200":
        description: Service dependencies are ready.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Readiness"
      "503":
        description: A required service dependency is unavailable.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Readiness"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: >-
        Opaque credential verified through the authentication port. Request
        payload identity or role fields are never trusted.

  parameters:
    SupplierID:
      name: supplierId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    VersionID:
      name: versionId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    DeletionOperationID:
      name: Idempotency-Key
      in: header
      required: true
      description: >-
        Caller-generated UUID that identifies one deletion attempt. Reuse the
        same value when retrying an ambiguous or interrupted request.
      schema:
        type: string
        format: uuid
    Query:
      name: q
      in: query
      description: Normalized, case-insensitive name or location search.
      schema:
        type: string
        maxLength: 120
    Type:
      name: type
      in: query
      description: Exact, case-insensitive supplier type.
      schema:
        type: string
        maxLength: 64
    Limit:
      name: limit
      in: query
      description: Maximum page size.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    Cursor:
      name: cursor
      in: query
      description: Opaque cursor returned by the preceding page.
      schema:
        type: string
        maxLength: 512

  schemas:
    SupplierFields:
      type: object
      required:
        - name
        - type
        - building
        - floor
        - locationDescription
        - latitude
        - longitude
        - openingTime
        - closingTime
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
          pattern: '\S'
        type:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '\S'
        building:
          type: string
          minLength: 1
          maxLength: 120
          pattern: '\S'
        floor:
          type: string
          minLength: 1
          maxLength: 32
          pattern: '\S'
        locationDescription:
          type: string
          minLength: 1
          maxLength: 500
          pattern: '\S'
        latitude:
          type: number
          format: double
          minimum: -90
          maximum: 90
        longitude:
          type: number
          format: double
          minimum: -180
          maximum: 180
        openingTime:
          type: string
          pattern: "^(?:[01][0-9]|2[0-3]):[0-5][0-9]$"
          examples: ["09:00"]
        closingTime:
          type: string
          pattern: "^(?:[01][0-9]|2[0-3]):[0-5][0-9]$"
          examples: ["18:00"]
        imageUrl:
          type: [string, "null"]
          format: uri
          maxLength: 2048
          pattern: '^https?://'

    SupplierWrite:
      allOf:
        - $ref: "#/components/schemas/SupplierFields"
      unevaluatedProperties: false

    SupplierPatch:
      type: object
      minProperties: 1
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
          pattern: '\S'
        type:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '\S'
        building:
          type: string
          minLength: 1
          maxLength: 120
          pattern: '\S'
        floor:
          type: string
          minLength: 1
          maxLength: 32
          pattern: '\S'
        locationDescription:
          type: string
          minLength: 1
          maxLength: 500
          pattern: '\S'
        latitude:
          type: number
          format: double
          minimum: -90
          maximum: 90
        longitude:
          type: number
          format: double
          minimum: -180
          maximum: 180
        openingTime:
          type: string
          pattern: "^(?:[01][0-9]|2[0-3]):[0-5][0-9]$"
        closingTime:
          type: string
          pattern: "^(?:[01][0-9]|2[0-3]):[0-5][0-9]$"
        imageUrl:
          type: [string, "null"]
          format: uri
          maxLength: 2048
          pattern: '^https?://'
      additionalProperties: false

    Supplier:
      allOf:
        - $ref: "#/components/schemas/SupplierFields"
        - type: object
          required: [supplierId, versionId, available, createdAt, updatedAt]
          properties:
            supplierId:
              type: string
              format: uuid
              readOnly: true
            versionId:
              type: string
              format: uuid
              readOnly: true
            available:
              type: boolean
              const: true
              readOnly: true
            createdAt:
              type: string
              format: date-time
              readOnly: true
            updatedAt:
              type: string
              format: date-time
              readOnly: true
      unevaluatedProperties: false

    SupplierVersion:
      allOf:
        - $ref: "#/components/schemas/SupplierFields"
        - type: object
          required: [supplierId, versionId, available, createdAt]
          properties:
            supplierId:
              type: string
              format: uuid
              readOnly: true
            versionId:
              type: string
              format: uuid
              readOnly: true
            available:
              type: boolean
              description: True only for the current version of a non-deleted supplier.
              readOnly: true
            createdAt:
              type: string
              format: date-time
              readOnly: true
      unevaluatedProperties: false

    SupplierPage:
      type: object
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/Supplier"
        nextCursor:
          type: string
          description: Omitted when there is no next page.

    ErrorField:
      type: object
      required: [field, message]
      properties:
        field:
          type: string
        message:
          type: string
      additionalProperties: false

    Error:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          enum:
            - INVALID_ARGUMENT
            - UNAUTHENTICATED
            - FORBIDDEN
            - SUPPLIER_NOT_FOUND
            - SUPPLIER_VERSION_NOT_FOUND
            - SUPPLIER_NAME_CONFLICT
            - SUPPLIER_HAS_ACTIVE_ERRANDS
            - DELETION_FENCE_UNAVAILABLE
            - DEPENDENCY_UNAVAILABLE
            - INTERNAL
        message:
          type: string
        fields:
          type: array
          items:
            $ref: "#/components/schemas/ErrorField"
      additionalProperties: false

    Readiness:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [ready, not_ready]
      additionalProperties: false

    DeletionPending:
      type: object
      required: [operationId, status]
      properties:
        operationId:
          type: string
          format: uuid
        status:
          type: string
          const: pending_fence_commit
      additionalProperties: false

  responses:
    InvalidArgument:
      description: The request or one or more named fields are invalid.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Error"
              - properties:
                  code:
                    const: INVALID_ARGUMENT
    Unauthenticated:
      description: A valid authenticated identity is required.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Error"
              - properties:
                  code:
                    const: UNAUTHENTICATED
    Forbidden:
      description: >-
        The authenticated identity lacks administrator permission or its
        authoritative account state denies access.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Error"
              - properties:
                  code:
                    const: FORBIDDEN
    SupplierNotFound:
      description: The supplier does not exist or has been deleted.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Error"
              - properties:
                  code:
                    const: SUPPLIER_NOT_FOUND
    SupplierVersionNotFound:
      description: The immutable supplier version does not exist.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Error"
              - properties:
                  code:
                    const: SUPPLIER_VERSION_NOT_FOUND
    NameConflict:
      description: The normalized name is used by another non-deleted supplier.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Error"
              - properties:
                  code:
                    const: SUPPLIER_NAME_CONFLICT
    ActiveErrands:
      description: One or more active errands still use this supplier.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Error"
              - properties:
                  code:
                    const: SUPPLIER_HAS_ACTIVE_ERRANDS
    DeletionFenceUnavailable:
      description: >-
        The fence could not be acquired or inspected safely. The supplier was
        not deleted by this operation.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Error"
              - properties:
                  code:
                    const: DELETION_FENCE_UNAVAILABLE
    DependencyUnavailable:
      description: A required dependency is unavailable.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Error"
              - properties:
                  code:
                    const: DEPENDENCY_UNAVAILABLE
    Internal:
      description: An unexpected internal error occurred.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/Error"
              - properties:
                  code:
                    const: INTERNAL
